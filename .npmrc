# Strict, reproducible installs. Cloudflare Workers Builds runs a workspace-wide
# install at the repo root, so keep the dependency footprint boring.
auto-install-peers=true
strict-peer-dependencies=false
shamefully-hoist=false
link-workspace-packages=true
prefer-workspace-packages=true
